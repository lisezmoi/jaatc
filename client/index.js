(function(window){
  var document = window.document,
      canv = document.getElementsByTagName('canvas')[0],
      ctx = canv.getContext('2d'),
      tileSize = 32;
  
  canv.width = tileSize * 25; // 800
  canv.height = tileSize * 15; // 480
  
  function drawRoom(room) {
    
  }
  
})(this);